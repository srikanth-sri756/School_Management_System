<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notes - Teacher Portal</title>
  <link rel="stylesheet" href="/css/modern.css">
</head>
<body>
  <%- include('../partials/teacher-navbar') %>
  <%- include('../partials/notification') %>

  <div class="container">
    <div class="page-header">
      <h2>Educational Notes</h2>
    </div>

    <!-- Add New Note -->
    <div class="card">
      <h3>Create New Note</h3>
      <form method="POST" action="/teacher/notes/add" enctype="multipart/form-data">
        <div class="form-group">
          <label>Note Title</label>
          <input type="text" name="title" required placeholder="Enter note title">
        </div>

        <div class="form-group">
          <label>Content</label>
          <textarea name="content" rows="8" required placeholder="Enter your note content"></textarea>
        </div>

        <div class="form-group">
          <label>Attach File (Optional)</label>
          <input type="file" name="attachment" accept=".pdf,.doc,.docx,.ppt,.pptx,.xls,.xlsx,.txt,.jpg,.jpeg,.png">
          <small style="display: block; margin-top: 5px; color: rgba(255,255,255,0.6);">
            Supported formats: PDF, DOC, DOCX, PPT, PPTX, XLS, XLSX, TXT, Images (Max: 10MB)
          </small>
        </div>

        <div class="form-group">
          <label>
            <input type="checkbox" name="isPublic" value="true">
            Make this note public to students
          </label>
        </div>

        <button type="submit" class="btn btn-primary">Create Note</button>
      </form>
    </div>

    <!-- View Existing Notes -->
    <div class="card">
      <h3>My Notes</h3>
      <% if (notes.length > 0) { %>
        <div class="notes-grid">
          <% notes.forEach(note => { %>
            <div class="note-item">
              <div class="note-header">
                <h4><%= note.title %></h4>
                <span class="badge <%= note.isPublic ? 'badge-public' : 'badge-private' %>">
                  <%= note.isPublic ? 'Public' : 'Private' %>
                </span>
              </div>
              <p class="note-content"><%= note.content %></p>
              
              <% if (note.attachment && note.attachment.filename) { %>
                <div class="note-attachment">
                  <i class="file-icon">📎</i>
                  <a href="/teacher/notes/<%= note._id %>/download" class="attachment-link">
                    <%= note.attachment.originalName %>
                  </a>
                  <small>(<%= (note.attachment.size / 1024).toFixed(2) %> KB)</small>
                </div>
              <% } %>
              
              <div class="note-footer">
                <small>
                  Subject: <%= note.subject.name %> | 
                  Class: <%= note.class.name %> | 
                  Created: <%= new Date(note.createdAt).toLocaleDateString() %>
                </small>
              </div>
              
              <div class="note-actions">
                <form method="POST" action="/teacher/notes/<%= note._id %>/delete" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this note?');">
                  <button type="submit" class="btn-delete">Delete</button>
                </form>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p>No notes created yet.</p>
      <% } %>
    </div>
  </div>

  <style>
    .notes-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }
    .note-item {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 12px;
      padding: 20px;
      backdrop-filter: blur(10px);
    }
    .note-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
    }
    .note-header h4 {
      margin: 0;
      color: white;
    }
    .note-content {
      color: rgba(255, 255, 255, 0.8);
      margin-bottom: 15px;
      line-height: 1.6;
    }
    .note-footer {
      color: rgba(255, 255, 255, 0.6);
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      padding-top: 10px;
      margin-top: 10px;
    }
    .note-attachment {
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.3);
      border-radius: 8px;
      padding: 10px 15px;
      margin: 15px 0;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .file-icon {
      font-size: 18px;
    }
    .attachment-link {
      color: #60a5fa;
      text-decoration: none;
      font-weight: 500;
      flex: 1;
    }
    .attachment-link:hover {
      color: #93c5fd;
      text-decoration: underline;
    }
    .note-actions {
      margin-top: 15px;
      padding-top: 10px;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      text-align: right;
    }
    .btn-delete {
      background-color: #ef4444;
      color: white;
      border: none;
      padding: 6px 16px;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      transition: all 0.3s;
    }
    .btn-delete:hover {
      background-color: #dc2626;
    }
    .badge {
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
    }
    .badge-public {
      background-color: #10b981;
      color: white;
    }
    .badge-private {
      background-color: #6b7280;
      color: white;
    }
  </style>

</body>
</html>
