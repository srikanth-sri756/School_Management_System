<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Holiday - School Management</title>
  <link rel="stylesheet" href="/css/modern.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  <%- include('../partials/notification') %>

  <div class="container">
    <div class="page-header">
      <h2>Add New Holiday</h2>
      <a href="/holidays" class="btn btn-secondary">Back to Holidays</a>
    </div>

    <div class="card">
      <form method="POST" action="/holidays/add" id="holidayForm">
        <div class="form-row">
          <div class="form-group">
            <label>Holiday Title <span style="color: #ef4444;">*</span></label>
            <input type="text" name="title" required placeholder="e.g., Independence Day">
          </div>

          <div class="form-group">
            <label>Date <span style="color: #ef4444;">*</span></label>
            <input type="date" name="date" id="holidayDate" required>
            <small id="dateWarning" style="display: none; color: #f59e0b; margin-top: 5px;"></small>
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label>Holiday Type</label>
            <select name="type" required>
              <option value="National">National Holiday</option>
              <option value="Religious">Religious Holiday</option>
              <option value="Festival">Festival</option>
              <option value="School Event">School Event</option>
              <option value="Other">Other</option>
            </select>
          </div>

          <div class="form-group">
            <label>
              <input type="checkbox" name="isRecurring" value="true">
              Recurring (Annual)
            </label>
            <small style="display: block; margin-top: 5px; color: rgba(255,255,255,0.6);">
              Check this if the holiday occurs every year on the same date
            </small>
          </div>
        </div>

        <div class="form-group">
          <label>Description (Optional)</label>
          <textarea name="description" rows="4" placeholder="Add any additional information about this holiday"></textarea>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary" id="submitBtn">Add Holiday</button>
          <a href="/holidays" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>

    <!-- Quick Info -->
    <div class="info-box">
      <h4>📋 Quick Information</h4>
      <ul>
        <li>Sundays are automatically marked as weekly offs</li>
        <li>Cannot add holidays on Sundays</li>
        <li>Only one holiday can be declared per day</li>
        <li>Attendance cannot be marked on holidays</li>
        <li>Mark as "Recurring" for annual holidays like Independence Day, Republic Day, etc.</li>
      </ul>
    </div>
  </div>

  <style>
    .form-row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
      margin-bottom: 20px;
    }
    .form-actions {
      display: flex;
      gap: 15px;
      margin-top: 30px;
    }
    .info-box {
      margin-top: 30px;
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.3);
      border-radius: 12px;
      padding: 25px;
    }
    .info-box h4 {
      color: white;
      margin: 0 0 15px 0;
      font-size: 18px;
    }
    .info-box ul {
      margin: 0;
      padding-left: 25px;
      color: rgba(255, 255, 255, 0.8);
    }
    .info-box li {
      margin-bottom: 8px;
      line-height: 1.6;
    }
  </style>

  <script>
    // Check if selected date is Sunday
    document.getElementById('holidayDate').addEventListener('change', function() {
      const selectedDate = new Date(this.value + 'T00:00:00');
      const dayOfWeek = selectedDate.getDay();
      const warning = document.getElementById('dateWarning');
      const submitBtn = document.getElementById('submitBtn');
      
      if (dayOfWeek === 0) {
        warning.textContent = '⚠️ Warning: This is a Sunday (already a weekly off). Please select a different date.';
        warning.style.display = 'block';
        submitBtn.disabled = true;
        submitBtn.style.opacity = '0.5';
        submitBtn.style.cursor = 'not-allowed';
      } else {
        warning.style.display = 'none';
        submitBtn.disabled = false;
        submitBtn.style.opacity = '1';
        submitBtn.style.cursor = 'pointer';
      }
    });

    // Prevent form submission if Sunday is selected
    document.getElementById('holidayForm').addEventListener('submit', function(e) {
      const selectedDate = new Date(document.getElementById('holidayDate').value + 'T00:00:00');
      if (selectedDate.getDay() === 0) {
        e.preventDefault();
        alert('Cannot add holiday on Sunday. Sundays are automatically weekly offs.');
      }
    });
  </script>

</body>
</html>
